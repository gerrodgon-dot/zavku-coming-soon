FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci --omit=dev --ignore-scripts && npm cache clean --force
COPY server ./server
USER node
ENV NODE_ENV=production PORT=8080
EXPOSE 8080
CMD ["node", "server/contact.mjs"]
